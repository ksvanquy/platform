import {
  hasPermission,
  requireRole,
  requireRoles,
  requirePermission,
  requirePermissions,
  requireAdmin,
  requireAuthor,
} from '@platform/security';

export {
  hasPermission,
  requireRole,
  requireRoles,
  requirePermission,
  requirePermissions,
  requireAdmin,
  requireAuthor,
};

